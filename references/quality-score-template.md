# QUALITY_SCORE.md 模板

用于生成 `docs/QUALITY_SCORE.md`。Lite 不默认生成。

```markdown
# 质量评分

## 总览

- 总体评分：{0-10 或待确认}
- 评分日期：{YYYY-MM-DD}
- 依据：{代码结构/测试/文档/配置}

## 维度评分

| 维度 | 分数 | 依据 | 下一步 |
| --- | --- | --- | --- |
| 架构清晰度 | `{分数}` | `{事实}` | `{建议}` |
| 测试可信度 | `{分数}` | `{事实}` | `{建议}` |
| 文档新鲜度 | `{分数}` | `{事实}` | `{建议}` |
| 安全基线 | `{分数}` | `{事实}` | `{建议}` |
| 可维护性 | `{分数}` | `{事实}` | `{建议}` |

## 优先改进项

1. `{问题}` - `{影响}` - `{建议}`
2. `{问题}` - `{影响}` - `{建议}`
3. `{问题}` - `{影响}` - `{建议}`

## 待确认

- `{无法从仓库确认的信息}`
```

## 评分原则

- 分数服务于维护决策，不制造仪式感。
- 没有足够事实时写 `待确认`，不要硬评分。
- Extended 项目可按前端、后端、测试、安全、运维展开。
