# ARCHITECTURE.md 模板

用于生成根目录 `ARCHITECTURE.md`。

```markdown
# 架构说明

## 系统概览

{用 3-6 句话说明项目职责、运行形态和主要边界。}

## 技术栈与运行形态

- 语言：{事实}
- 框架：{事实}
- 包管理/构建：{事实}
- 运行入口：{事实}
- 部署形态：{事实或待确认}

## 目录与模块地图

| 路径 | 职责 | 备注 |
| --- | --- | --- |
| `{路径}` | `{职责}` | `{事实或待确认}` |

## 层级边界

| 层级/模块 | 职责 | 可以依赖 | 不应依赖 |
| --- | --- | --- | --- |
| `{名称}` | `{职责}` | `{允许}` | `{禁止}` |

## 关键数据流

1. `{入口}` 接收 `{输入}`。
2. `{处理层}` 执行 `{业务处理}`。
3. `{数据/外部系统}` 完成 `{读写或调用}`。
4. `{出口}` 返回 `{输出}`。

## 外部系统与集成

| 系统 | 用途 | 调用位置 | 风险 |
| --- | --- | --- | --- |
| `{系统}` | `{用途}` | `{路径}` | `{风险或待确认}` |

## 架构不变量

- `{不变量 1}`
- `{不变量 2}`
- `{不变量 3}`

## 前后端一体边界

{仅当前后端一体项目时生成。写清前端入口、后端入口、API 契约、静态资源托管、构建产物流向和部署关系。}

## 待确认事项

- `{无法从代码确认的信息}`
```

## 写作要求

- 使用项目真实命名。
- 不写未来愿景，除非标记为 `待确认`。
- 不把开发命令写在这里；命令进入 `docs/ENGINEERING.md`。
