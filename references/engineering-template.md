# ENGINEERING.md 模板

用于生成 `docs/ENGINEERING.md`。

````markdown
# 工程开发指南

## 环境要求

- 运行时：{Node/Python/JDK/Go/etc}
- 包管理器：{事实}
- 系统依赖：{事实或待确认}

## 安装依赖

- 命令：`{真实命令}`
- 来源：`{配置文件或文档}`

## 本地运行

- 命令：`{真实命令}`
- 来源：`{配置文件或文档}`

## 测试、Lint、Format、Build

| 任务 | 命令 | 来源 | 覆盖范围 | 备注 |
| --- | --- | --- | --- | --- |
| 测试 | `{真实命令或待确认}` | `{package.json/Makefile/etc}` | `{目录/文件/待确认}` | `{风险或待确认}` |
| Lint | `{真实命令或待确认}` | `{来源}` | `{目录/文件/待确认}` | `{风险或待确认}` |
| Format | `{真实命令或待确认}` | `{来源}` | `{目录/文件/待确认}` | `{风险或待确认}` |
| Build | `{真实命令或待确认}` | `{来源}` | `{目录/文件/待确认}` | `{风险或待确认}` |

## 前后端一体开发

{仅前后端一体项目生成。分别列出前端和后端命令、统一入口、本地联调方式、端口和代理关系。}

## 开发规范入口

- 代码规范：`docs/standards/coding.md`
- 提交规范：`docs/standards/commits.md`

## 代码组织约定

- `{约定}`

## 常见故障排查

| 问题 | 处理方式 |
| --- | --- |
| `{问题}` | `{处理方式或待确认}` |
````

## 写作要求

- 所有命令必须来自真实配置。
- 如果命令缺失，写 `待确认`，不要发明。
- 测试、lint、format、build 必须记录覆盖范围；覆盖范围不清楚时写 `待确认`。
- 如果命令只覆盖单个入口文件或部分源码，必须在备注中标出风险。
- 如果目标项目已有 README 中的运行方式，优先吸收并去重。
